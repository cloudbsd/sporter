<% content_for :header do %>
  <span class='devise-button'>
    <!-- 放弃当前修改 -->
    <%= link_to t('layouts.back'), :back, class: 'btn btn-warning' %>
    <%= link_to t('devise.logoff'), registration_path(resource_name), class: 'btn btn-danger', :data => { confirm: t('layouts.confirm') }, :method => :delete %>
  </span>
<% end %>

<%# render partial: 'shared/subheader_user', locals: { user: @user, title: t('menubar.user.edit_emailpw') } %>

<div class='row'>
  <div class='col-md-8'>
    <%= render 'devise/shared/info' %>
  </div>
  <div class='col-md-4'>
    <%= render 'form_edit' %>
  </div>
</div> <!-- .row -->
